# Linux 磁盘分区与挂载

## 概念

| 概念 | 说明 |
|------|------|
| 分区 | 将一块物理磁盘划分为多个独立区域，每个区域可单独管理 |
| 文件系统 | 分区上的数据组织方式（ext4、xfs 等） |
| 挂载 (mount) | 将分区关联到某个目录，通过该目录访问分区内容 |
| 挂载点 | 分区挂载到的目录（如 `/`、`/home`） |
| 卸载 (umount) | 断开分区与目录的关联 |
| `/etc/fstab` | 系统开机时自动挂载的配置文件 |

## 分区类型

| 类型 | 说明 |
|------|------|
| MBR (DOS) | 传统分区表，最多 4 个主分区，支持 ≤2TB 磁盘 |
| GPT | 现代分区表，支持 128 个分区，支持 >2TB 磁盘 |

## 命名规则

| 设备 | 含义 |
|------|------|
| `sda` | 第一块 SATA/SAS 硬盘 |
| `sdb` | 第二块 SATA/SAS 硬盘 |
| `sda1` | 第一块硬盘的第一个分区 |
| `sda2` | 第一块硬盘的第二个分区 |
| `nvme0n1` | 第一块 NVMe SSD |
| `nvme0n1p1` | 第一块 NVMe SSD 的第一个分区 |

## 常用文件系统

| 类型 | 特点 |
|------|------|
| `ext4` | Linux 默认，稳定，支持最大 1EB |
| `xfs` | 高性能，适合大文件，CentOS/RHEL 默认 |
| `btrfs` | 支持快照、压缩，现代特性 |
| `swap` | 交换分区，当内存不足时使用 |
| `vfat`/`ntfs` | Windows 兼容，U 盘常用 |

## 指令

| 命令 | 作用 | 常用选项 |
|------|------|----------|
| `lsblk` | 列出所有块设备 | `-f` 显示文件系统类型 |
| `fdisk -l` | 查看磁盘分区表 | |
| `fdisk /dev/sdb` | 进入交互式分区工具 | `n` 新建，`d` 删除，`w` 写入，`q` 退出 |
| `parted` | GPT 分区工具 | 支持大于 2TB 磁盘 |
| `mkfs.ext4 /dev/sdb1` | 格式化为 ext4 | 也可 `mkfs.xfs`、`mkfs.vfat` |
| `mkswap /dev/sdb1` | 格式化为 swap | |
| `swapon /dev/sdb1` | 启用 swap | |
| `mount /dev/sdb1 /mnt` | 挂载分区到目录 | `-t ext4` 指定文件系统类型 |
| `umount /mnt` | 卸载分区 | `-l` 懒卸载（繁忙时强制） |
| `df -h` | 查看挂载及空间 | |
| `blkid` | 查看分区 UUID | 配合 `/etc/fstab` 使用 |

## 操作流程

```
1. lsblk                     # 确认磁盘名
2. fdisk /dev/sdb            # 创建分区
3. mkfs.ext4 /dev/sdb1       # 格式化
4. mount /dev/sdb1 /mnt      # 挂载
5. df -h                     # 确认挂载成功
```

## /etc/fstab 自动挂载

| 字段 | 含义 | 示例 |
|------|------|------|
| 设备 | UUID 或设备路径 | `UUID=xxx` 或 `/dev/sdb1` |
| 挂载点 | 目标目录 | `/mnt/data` |
| 文件系统 | 类型 | `ext4` |
| 选项 | 挂载参数 | `defaults` |
| dump | 备份标记 | `0` |
| pass | 开机检查顺序 | `0` 跳过，`1` 根分区，`2` 其他 |
