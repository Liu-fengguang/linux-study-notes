# Vim 使用笔记

## 三种模式

| 模式 | 进入方式 | 作用 |
|------|------|------|
| 普通 (Normal) | `Esc` | 移动、删除、复制、粘贴 |
| 插入 (Insert) | `i` `a` `o` | 编辑文本 |
| 命令 (Command) | `:` | 保存、退出、搜索替换 |

> `Esc` 或 `Ctrl+[` 回到普通模式

## 进入插入模式

| 命令 | 位置 |
|------|------|
| `i` | 光标前插入 |
| `a` | 光标后插入 |
| `o` | 下一行插入 |
| `O` | 上一行插入 |
| `I` | 行首插入 |
| `A` | 行尾插入 |

## 移动（普通模式）

| 命令 | 动作 |
|------|------|
| `h` `j` `k` `l` | 左 下 上 右 |
| `w` / `b` | 下一个词 / 上一个词 |
| `0` / `$` | 行首 / 行尾 |
| `gg` / `G` | 文件开头 / 文件末尾 |
| `:n` | 跳到第 n 行 |
| `Ctrl+f` / `Ctrl+b` | 下一页 / 上一页 |

## 编辑

| 命令 | 作用 |
|------|------|
| `x` | 删除当前字符 |
| `dd` | 删除整行 |
| `dw` | 删除一个词 |
| `yy` | 复制整行 |
| `p` / `P` | 粘贴到下一行 / 上一行 |
| `u` | 撤销 |
| `Ctrl+r` | 重做 |

## 搜索与替换

| 命令 | 作用 |
|------|------|
| `/keyword` | 搜索，`n` 下一处，`N` 上一处 |
| `:%s/old/new/g` | 全文替换 |
| `:%s/old/new/gc` | 全文替换（逐个确认） |
| `:s/old/new/g` | 仅替换当前行 |

## 保存与退出

| 命令 | 作用 |
|------|------|
| `:w` | 保存 |
| `:q` | 退出 |
| `:wq` 或 `ZZ` | 保存并退出 |
| `:q!` | 不保存强制退出 |
| `:w !sudo tee %` | 忘记 sudo 时的补救保存 |

## 使用建议

| 建议 | 原因 |
|------|------|
| 用 `hjkl` 代替方向键 | 手不离开主键盘区 |
| 少用 `i` 多用 `o` 和 `A` | 减少移动后再插入 |
| 别死记，每天练 10 分钟 | 肌肉记忆比记忆表管用 |
| 数字组合：`5dd` 删 5 行，`3p` 粘 3 次 | 省时 |

> 内置教程：终端输入 `vimtutor`，30 分钟过一遍比看任何笔记都有效。
