# Linux 目录结构

Linux 遵循 **FHS（Filesystem Hierarchy Standard，文件系统层次结构标准）**，所有目录和文件都从根目录 `/` 开始，形成一棵倒置的树。

```
/
├── bin/          # 基本用户命令（所有用户可用）
├── sbin/         # 系统管理命令（需 root 权限）
├── boot/         # 启动文件（内核、引导程序）
├── dev/          # 设备文件
├── etc/          # 系统配置文件
├── home/         # 普通用户的家目录
├── root/         # root 用户的家目录
├── lib/          # 共享库和内核模块
├── media/        # 自动挂载的可移动设备（U盘、光驱）
├── mnt/          # 手动临时挂载点
├── opt/          # 第三方软件安装目录
├── proc/         # 进程和系统信息的虚拟文件系统
├── sys/          # 内核和硬件信息的虚拟文件系统
├── run/          # 运行时临时数据（重启后清空）
├── srv/          # 服务数据目录
├── tmp/          # 临时文件（重启后可能清空）
├── usr/          # 用户程序和数据的二级目录
│   ├── bin/      #   非必需的用户命令
│   ├── sbin/     #   非必需的系统管理命令
│   ├── lib/      #   库文件
│   ├── include/  #   C/C++ 头文件
│   ├── share/    #   架构无关的共享数据
│   └── local/    #   本地编译安装的软件
└── var/          # 可变数据（日志、缓存、数据库）
    ├── log/      #   系统日志
    ├── cache/    #   应用程序缓存
    ├── spool/    #   待处理任务队列（邮件、打印）
    └── tmp/      #   持久化的临时文件
```

---

## 核心目录详解

### `/` — 根目录
整个系统的起点，所有目录和文件都在其下。只有 root 用户有权在 `/` 下直接创建文件。

### `/bin` — 基本用户命令
存放系统启动和单用户模式下需要的基本命令，所有用户都能使用。

> `ls`、`cp`、`mv`、`cat`、`mkdir`、`rm` 等

现代 Linux 发行版（如 Ubuntu 20.04+）通常将 `/bin` 软链接到 `/usr/bin`。

### `/sbin` — 系统管理命令
存放需要 **root 权限** 才能执行的系统管理命令。

> `fdisk`、`mkfs`、`ifconfig`、`iptables`、`reboot` 等

### `/boot` — 启动文件
存放系统启动所需的文件：
- **vmlinuz-xxx** — Linux 内核镜像
- **initrd.img-xxx** — 初始化 RAM 磁盘镜像
- **grub/** — GRUB 引导程序配置文件

> 不要手动删除此目录中的文件，否则系统无法启动。

### `/dev` — 设备文件
Linux 将硬件设备抽象为文件，所有设备都在此目录下：

| 文件 | 对应设备 |
|------|----------|
| `sda` | 第一块 SATA/SCSI 硬盘 |
| `sda1` | 第一块硬盘的第一个分区 |
| `tty` | 终端 |
| `null` | 黑洞设备（丢弃所有写入） |
| `random` | 随机数生成器 |

### `/etc` — 系统配置
存放所有系统级配置文件和启动脚本。

> `passwd`（用户信息）、`fstab`（文件系统挂载表）、`hostname`（主机名）、`ssh/`（SSH 配置）、`nginx/`（Nginx 配置）

### `/home` — 普通用户家目录
每个普通用户在此目录下拥有以自己用户名命名的子目录，存放个人文件和自己软件的配置。

> `/home/alice`、`/home/bob`

用户级配置文件以隐藏文件（`.` 开头）存储在各自的 home 目录下。

### `/root` — root 用户家目录
root 超级用户的家目录，与 `/home` 分开存放，确保在 `/home` 分区故障时 root 仍可登录系统。

### `/lib` — 共享库
存放 `/bin`、`/sbin` 中的程序所需的共享库文件（`.so`），以及内核模块（`/lib/modules/`）。

> `/lib/x86_64-linux-gnu/` 存放 64 位库文件

### `/proc` — 进程信息虚拟文件系统
**虚拟文件系统**，数据存在于内存中，反映系统运行状态：

| 文件/目录 | 内容 |
|-----------|------|
| `/proc/cpuinfo` | CPU 信息 |
| `/proc/meminfo` | 内存信息 |
| `/proc/version` | 内核版本 |
| `/proc/<PID>/` | 进程详细信息 |
| `/proc/uptime` | 系统运行时间 |

### `/sys` — 内核信息虚拟文件系统
向用户空间暴露内核对象信息，主要用于设备和驱动管理。

### `/tmp` — 临时文件
任何用户和程序都可以在此存放临时文件，系统重启后**可能被清空**。需要持久化的临时文件应放在 `/var/tmp`。

### `/usr` — 用户程序
多数用户级程序和工具都在此目录下：

| 子目录 | 作用 |
|--------|------|
| `/usr/bin` | 用户命令（非启动必需） |
| `/usr/sbin` | 系统管理命令（非启动必需） |
| `/usr/lib` | 库文件 |
| `/usr/include` | C/C++ 头文件 |
| `/usr/share` | 帮助文档、图标等架构无关数据 |
| `/usr/local` | 本地编译安装的软件 |

> 装软件到 `/opt` 还是 `/usr/local`？
> - `/opt`：第三方闭源或独立软件（如 IDE、浏览器）
> - `/usr/local`：从源码编译安装的软件

### `/var` — 可变数据
存放运行过程中会持续变化的数据：

| 子目录 | 作用 |
|--------|------|
| `/var/log` | 系统和应用日志文件 |
| `/var/spool` | 打印、邮件等待处理队列 |
| `/var/cache` | 应用程序缓存 |
| `/var/lib` | 应用状态数据（如数据库文件） |

---

## 快速记忆口诀

- **一切从 `/` 开始** — 根目录是全部
- **`/etc` 管配置** — 改设置找这里
- **`/var` 管变化** — 日志、缓存、数据都在此
- **`/home` 是你的家** — 个人文件放这里
- **`/proc` 和 `/sys`** — 不占硬盘，内存虚拟，窥探系统运行状态
- **`/tmp` 是临时区** — 重启就没了
- **`/opt` 装外来软件** — 第三方大型软件安身之所
